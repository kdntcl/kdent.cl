[build]
  command = "npm run build"
  publish = "dist"

# Plugin de prerenderizado de Netlify
[[plugins]]
  package = "netlify-plugin-prerender-spa"

# Configuración para React SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


