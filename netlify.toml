[build]
  command = "npm run build"
  publish = "dist"

# Prerenderizado automático de Netlify para SPA
[[plugins]]
  package = "@netlify/plugin-prerender-spa"

# Configuración para React SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


